# ElevateLabs_Task-3
Extracted a dataset from Kaggle on sugar consumption by people from around the world. Used SQL queries to make the data more readable. 
Utilised queries such as the ones showing contrast between WHERE and HAVING, demonstrate every join type, compute an “ARPU” analogue, use sub‑queries, show optimisation techniques, build a view, and handle NULLs. 

